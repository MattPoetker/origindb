version: '3.8'

services:
  origindb:
    image: origindb/origindb:latest
    container_name: origindb-server
    ports:
      - "8080:8080"    # WebSocket port
      - "50051:50051"  # gRPC port
    volumes:
      - origindb_data:/data
      - ./logs:/logs:rw
    environment:
      - ORIGINDB_LOG_LEVEL=info
      - ORIGINDB_WS_PORT=8080
      - ORIGINDB_GRPC_PORT=50051
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "origindb_sql", "SELECT 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Nginx reverse proxy for production
  nginx:
    image: nginx:alpine
    container_name: origindb-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/ssl/certs:ro
    depends_on:
      - origindb
    restart: unless-stopped
    profiles:
      - production

  # Optional: Prometheus monitoring
  prometheus:
    image: prom/prometheus:latest
    container_name: origindb-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    profiles:
      - monitoring

  # Optional: Grafana for metrics visualization
  grafana:
    image: grafana/grafana:latest
    container_name: origindb-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./grafana/datasources:/etc/grafana/provisioning/datasources:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    restart: unless-stopped
    profiles:
      - monitoring

volumes:
  origindb_data:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local

networks:
  default:
    name: origindb-network